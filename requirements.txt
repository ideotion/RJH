# Core dependencies — required to run RJH.
fastapi
uvicorn
requests

# Optional extras. Uncomment a line (or `pip install` it) only if you want that
# feature; RJH runs fully without them.
#
# Browser pre-fill assistant. After installing, fetch a browser engine with:
#   playwright install firefox      (or: chromium / webkit)
# playwright
#
# Import resumes from PDF / ODT files (parsed locally):
# pypdf
# odfpy
